import { Order } from "@/types/orders";
import { StatusBadge } from "./status-badge";

export function RecentOrdersTable({ orders }: { orders: Order[] }) {
  return <section className="rounded-[20px] border border-slate-100 bg-white p-5 shadow-[0_18px_45px_rgba(15,23,42,0.06)]"><div className="mb-4 flex items-center justify-between"><h2 className="text-lg font-black">Recent orders</h2><span className="text-sm text-slate-500">Latest {orders.length}</span></div><div className="hidden overflow-x-auto md:block"><table className="min-w-full text-left text-sm"><thead className="text-xs uppercase text-slate-400"><tr><th className="py-2">Order</th><th>Revenue</th><th>Status</th><th>Created</th></tr></thead><tbody>{orders.slice(0, 6).map((order) => <tr key={order.id} className="border-t border-slate-100"><td className="py-3 font-bold">{order.order_number}</td><td>${order.revenue}</td><td><StatusBadge value={order.status} /></td><td className="text-slate-500">{new Date(order.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div><div className="grid gap-3 md:hidden">{orders.slice(0, 6).map((order) => <article key={order.id} className="rounded-2xl border border-slate-100 p-4"><div className="flex items-center justify-between gap-3"><strong>{order.order_number}</strong><StatusBadge value={order.status} /></div><p className="mt-2 text-sm text-slate-500">Revenue ${order.revenue}</p></article>)}</div></section>;
}
