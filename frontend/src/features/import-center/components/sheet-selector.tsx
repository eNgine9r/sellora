export function SheetSelector({ sheets, value, onChange }: { sheets: string[]; value: string; onChange: (sheet: string) => void }) { return <select className="w-full min-w-0 max-w-full rounded-md border border-slate-300 px-3 py-2" value={value} onChange={(event) => onChange(event.target.value)}><option value="">Select sheet</option>{sheets.map((sheet) => <option key={sheet} value={sheet}>{sheet}</option>)}</select>; }
