import { ValidationIssue } from "@/types/import-center";
export function ValidationIssuesTable({ issues }: { issues: ValidationIssue[] }) { if (!issues.length) return null; return <div className="rounded-xl bg-white p-4 shadow-sm"><h2 className="mb-3 font-semibold">Validation issues</h2><table className="w-full min-w-[640px] text-left text-sm"><thead><tr className="text-slate-500"><th>Row</th><th>Severity</th><th>Field</th><th>Message</th></tr></thead><tbody>{issues.map((issue, index) => <tr className="border-t" key={index}><td className="py-2">{issue.row_number ?? "Mapping"}</td><td>{issue.severity}</td><td>{issue.field}</td><td>{issue.message}</td></tr>)}</tbody></table></div>; }
