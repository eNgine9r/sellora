const fields: Record<string, string[]> = { customers: ["name", "phone", "instagram_username", "city", "region"], products: ["name", "sku", "description"], product_variants: ["product_name", "product_sku", "variant_sku", "color", "size", "selling_price"], inventory: ["variant_sku", "stock_quantity", "reserved_quantity", "minimum_quantity"], orders: ["customer_name", "customer_phone", "instagram_username", "revenue", "order_total", "created_at", "order_date", "ad_cost", "shipping_cost", "cod_fee", "other_cost", "net_profit"] };
export function ColumnMappingForm({ entityType, columns, mapping, onChange }: { entityType: string; columns: string[]; mapping: Record<string, string>; onChange: (mapping: Record<string, string>) => void }) { return <div className="rounded-xl bg-white p-4 shadow-sm"><h2 className="mb-3 font-semibold">Column mapping</h2><div className="grid gap-3 md:grid-cols-2">{(fields[entityType] ?? []).map((field) => <label className="grid gap-1 text-sm" key={field}><span>{field}</span><select className="rounded-md border border-slate-300 px-2 py-1" value={mapping[field] ?? ""} onChange={(event) => onChange({ ...mapping, [field]: event.target.value })}><option value="">Not mapped</option>{columns.map((column) => <option key={column} value={column}>{column}</option>)}</select></label>)}</div></div>; }
