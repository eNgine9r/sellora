import { ValidationReport as Report } from "@/types/import-center";
export function ValidationReport({ report }: { report?: Report }) { if (!report) return null; return <section className="rounded-xl bg-white p-4 shadow-sm"><h2 className="font-semibold">Validation report</h2><p className={report.is_valid ? "text-green-600" : "text-red-600"}>{report.is_valid ? "Valid" : "Invalid"} · {report.total_rows} rows</p>{report.errors.map((error) => <p className="text-sm text-red-600" key={error}>{error}</p>)}{report.warnings.map((warning) => <p className="text-sm text-amber-600" key={warning}>{warning}</p>)}</section>; }
