import { ValidationReport as Report } from "@/types/import-center";
export function ValidationReport({ report }: { report?: Report }) { if (!report) return null; return <section className="w-full min-w-0 max-w-full overflow-hidden rounded-xl bg-white p-4 shadow-sm"><h2 className="font-semibold">Validation report</h2><p className={report.is_valid ? "break-words text-green-600" : "break-words text-red-600"}>{report.is_valid ? "Valid" : "Invalid"} · {report.total_rows} rows</p>{report.errors.map((error) => <p className="break-words text-sm text-red-600" key={error}>{error}</p>)}{report.warnings.map((warning) => <p className="break-words text-sm text-amber-600" key={warning}>{warning}</p>)}</section>; }
