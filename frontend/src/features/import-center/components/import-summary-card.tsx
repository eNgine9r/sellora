import { ImportJob } from "@/types/import-center";
import { ImportJobStatusBadge } from "@/features/import-center/components/import-job-status-badge";
export function ImportSummaryCard({ job }: { job?: ImportJob | null }) { if (!job) return null; return <section className="w-full min-w-0 max-w-full overflow-hidden rounded-xl bg-white p-4 shadow-sm"><div className="flex min-w-0 flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><h2 className="min-w-0 truncate font-semibold" title={job.file_name}>{job.file_name}</h2><ImportJobStatusBadge status={job.status} /></div><div className="mt-3 grid min-w-0 gap-2 text-sm sm:grid-cols-2 lg:grid-cols-4"><span>Total {job.total_rows}</span><span>Processed {job.processed_rows}</span><span>Success {job.success_rows}</span><span>Failed {job.failed_rows}</span></div></section>; }
