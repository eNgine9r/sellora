export function ImportUploadCard({ onUpload }: { onUpload: (file: File) => void }) { return <section className="rounded-xl bg-white p-4 shadow-sm"><h2 className="font-semibold">Upload Excel file</h2><input className="mt-3 block w-full" type="file" accept=".xlsx" onChange={(event) => { const file = event.target.files?.[0]; if (file) onUpload(file); }} /><p className="mt-2 text-sm text-slate-500">Only .xlsx is supported in MVP. Files are stored locally and paths are never shown.</p></section>; }
