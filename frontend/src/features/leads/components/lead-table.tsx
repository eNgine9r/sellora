import { Lead, LeadSource } from "@/types/crm";
import { useI18n } from "@/i18n/provider";
import { LeadStatusBadge } from "./lead-status-badge";

function nextActionKey(status: Lead["status"]) {
  if (status === "NEW" || status === "IN_PROGRESS") return "leads.nextActionContact";
  if (status === "QUALIFIED") return "leads.nextActionCreateOrder";
  if (status === "CONVERTED") return "leads.nextActionConverted";
  return "leads.nextActionLost";
}

export function LeadTable({ leads, leadSources, onEdit, onArchive }: { leads: Lead[]; leadSources: LeadSource[]; onEdit?: (lead: Lead) => void; onArchive?: (lead: Lead) => void }) {
  const { t } = useI18n();
  const sourceById = new Map(leadSources.map((source) => [source.id, source.name]));
  const sourceName = (lead: Lead) => lead.lead_source_id ? sourceById.get(lead.lead_source_id) ?? t("leads.unknownSource") : "—";

  return <div className="mobile-card-list w-full min-w-0 max-w-full rounded-xl border border-slate-200 bg-white p-3 shadow-sm dark:border-white/10 dark:bg-slate-900"><div className="sellora-scrollbar hidden overflow-x-auto md:block"><table className="w-full min-w-[920px] divide-y divide-slate-200 text-sm"><thead className="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">{t("tables.instagram")}</th><th className="px-4 py-3">{t("tables.name")}</th><th className="px-4 py-3">{t("tables.phone")}</th><th className="px-4 py-3">{t("tables.status")}</th><th className="px-4 py-3">{t("leads.source")}</th><th className="px-4 py-3">{t("leads.campaignLabel")}</th><th className="px-4 py-3">{t("leads.assignedManager")}</th><th className="px-4 py-3">{t("leads.nextAction")}</th><th className="px-4 py-3">{t("leads.createdAt")}</th><th className="px-4 py-3">{t("tables.actions")}</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-white/10">{leads.map((lead) => <tr key={lead.id} className="hover:bg-slate-50 dark:hover:bg-white/[0.04]"><td className="px-4 py-3 text-slate-700 dark:text-slate-200">{lead.instagram_username ? `@${lead.instagram_username.replace(/^@/, "")}` : "—"}</td><td className="px-4 py-3 font-medium text-slate-900 dark:text-white">{lead.name}</td><td className="px-4 py-3 text-slate-700 dark:text-slate-200">{lead.phone ?? "—"}</td><td className="px-4 py-3"><LeadStatusBadge status={lead.status} /></td><td className="px-4 py-3 text-slate-700 dark:text-slate-200">{sourceName(lead)}</td><td className="px-4 py-3 text-slate-700 dark:text-slate-200">{lead.campaign_name ?? "—"}</td><td className="px-4 py-3 text-slate-700 dark:text-slate-200">{lead.assigned_user_id ?? t("leads.unassigned")}</td><td className="max-w-[220px] px-4 py-3 text-slate-700 dark:text-slate-200">{t(nextActionKey(lead.status))}</td><td className="px-4 py-3 text-slate-700 dark:text-slate-200">{new Date(lead.created_at).toLocaleDateString()}</td><td className="px-4 py-3"><div className="flex flex-wrap gap-2">{onEdit ? <button aria-label={`${t("leads.edit")} ${lead.name}`} className="rounded-lg border border-slate-300 px-3 py-2 font-semibold dark:border-white/10" onClick={() => onEdit(lead)}>{t("leads.edit")}</button> : <span className="text-slate-400">{t("common.readOnly")}</span>}{onArchive ? <button aria-label={`${t("leads.archive")} ${lead.name}`} className="rounded-lg border border-rose-200 px-3 py-2 font-semibold text-rose-700 dark:border-rose-400/40 dark:text-rose-200" onClick={() => onArchive(lead)}>{t("leads.archive")}</button> : null}</div></td></tr>)}{leads.length === 0 ? <tr><td className="px-4 py-8 text-center text-slate-500" colSpan={10}>{t("leads.empty")}</td></tr> : null}</tbody></table></div><div className="grid gap-3 md:hidden">{leads.map((lead) => <article key={lead.id} className="mobile-lead-card min-w-0 rounded-2xl border border-slate-200 p-4 dark:border-white/10"><div className="flex min-w-0 items-start justify-between gap-3"><div className="min-w-0"><p className="truncate text-sm font-bold text-violet-700 dark:text-violet-200">{lead.instagram_username ? `@${lead.instagram_username.replace(/^@/, "")}` : sourceName(lead)}</p><h3 className="mt-1 truncate text-lg font-black text-slate-950 dark:text-white">{lead.name}</h3><p className="truncate text-sm text-slate-500 dark:text-slate-300">{t("tables.phone")}: {lead.phone ?? "—"}</p></div><LeadStatusBadge status={lead.status} /></div><div className="mt-3 grid gap-1 text-sm text-slate-600 dark:text-slate-300"><span>{t("leads.source")}: {sourceName(lead)}</span><span>{t("leads.campaignLabel")}: {lead.campaign_name ?? "—"}</span><span>{t("leads.nextAction")}: {t(nextActionKey(lead.status))}</span><span>{t("leads.createdAt")}: {new Date(lead.created_at).toLocaleDateString()}</span></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{onEdit ? <button aria-label={`${t("leads.edit")} ${lead.name}`} className="min-h-11 rounded-xl border border-slate-300 px-4 py-3 font-bold dark:border-white/10" onClick={() => onEdit(lead)}>{t("leads.edit")}</button> : <span className="text-sm text-slate-400">{t("common.readOnly")}</span>}{onArchive ? <button aria-label={`${t("leads.archive")} ${lead.name}`} className="min-h-11 rounded-xl border border-rose-200 px-4 py-3 font-bold text-rose-700 dark:border-rose-400/40 dark:text-rose-200" onClick={() => onArchive(lead)}>{t("leads.archive")}</button> : null}</div></article>)}{leads.length === 0 ? <p className="p-6 text-center text-slate-500">{t("leads.empty")}</p> : null}</div></div>;
}
