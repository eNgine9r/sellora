import { AdCampaign } from "@/types/advertising";
export function CampaignTable({ campaigns, onEdit }: { campaigns: AdCampaign[]; onEdit?: (campaign: AdCampaign) => void }) { return <section className="rounded-2xl bg-white p-4 shadow-sm"><h2 className="text-lg font-semibold">Campaigns</h2><div className="mt-3 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b text-slate-500"><th>Name</th><th>Platform</th><th>Status</th><th>Objective</th><th>Daily Budget</th><th>Total Budget</th><th>Start Date</th><th>End Date</th><th>Actions</th></tr></thead><tbody>{campaigns.map((campaign) => <tr key={campaign.id} className="border-b last:border-0"><td className="py-2 font-medium">{campaign.name}</td><td>{campaign.platform}</td><td>{campaign.status}</td><td>{campaign.objective}</td><td>{campaign.daily_budget ?? "—"}</td><td>{campaign.total_budget ?? "—"}</td><td>{campaign.start_date ?? "—"}</td><td>{campaign.end_date ?? "—"}</td><td><button className="rounded-lg border border-slate-300 px-3 py-2 font-semibold" onClick={() => onEdit?.(campaign)}>Edit</button></td></tr>)}</tbody></table></div></section>; }
