import { AdCampaign } from "@/types/advertising";
export function CampaignTable({ campaigns, onEdit, onArchive }: { campaigns: AdCampaign[]; onEdit?: (campaign: AdCampaign) => void; onArchive?: (campaign: AdCampaign) => void }) { return <section className="w-full min-w-0 max-w-full overflow-hidden rounded-2xl bg-white p-4 shadow-sm"><h2 className="text-lg font-semibold">Campaigns</h2><div className="mt-3 max-w-full overflow-x-auto"><table className="w-full min-w-[640px] text-left text-sm"><thead><tr className="border-b text-slate-500"><th>Name</th><th>Platform</th><th>Status</th><th>Objective</th><th>Daily Budget</th><th>Total Budget</th><th>Start Date</th><th>End Date</th><th>Actions</th></tr></thead><tbody>{campaigns.map((campaign) => <tr key={campaign.id} className="border-b last:border-0"><td className="py-2 font-medium">{campaign.name}</td><td>{campaign.platform}</td><td>{campaign.status}</td><td>{campaign.objective}</td><td>{campaign.daily_budget ?? "—"}</td><td>{campaign.total_budget ?? "—"}</td><td>{campaign.start_date ?? "—"}</td><td>{campaign.end_date ?? "—"}</td><td><div className="flex flex-wrap gap-2">{onEdit ? <button aria-label={`Edit campaign ${campaign.name}`} className="rounded-lg border border-slate-300 px-3 py-2 font-semibold" onClick={() => onEdit(campaign)}>Edit campaign</button> : <span className="text-slate-400">Read-only</span>}{onArchive ? <button aria-label={`Archive campaign ${campaign.name}`} className="rounded-lg border border-rose-200 px-3 py-2 font-semibold text-rose-700" onClick={() => onArchive(campaign)}>Archive campaign</button> : null}</div></td></tr>)}</tbody></table></div></section>; }
