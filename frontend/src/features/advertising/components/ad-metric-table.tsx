import { AdMetric } from "@/types/advertising";
import { MetricValue } from "./metric-value";
export function AdMetricTable({ metrics, onEdit, onDelete }: { metrics: AdMetric[]; onEdit?: (metric: AdMetric) => void; onDelete?: (metric: AdMetric) => void }) { return <section className="w-full min-w-0 max-w-full overflow-hidden rounded-2xl bg-white p-4 shadow-sm"><h2 className="text-lg font-semibold">Daily Metrics</h2><div className="sellora-scrollbar mt-3 max-w-full overflow-x-auto"><table className="w-full min-w-[640px] text-left text-sm"><thead><tr className="border-b text-slate-500"><th>Date</th><th>Spend</th><th>Impressions</th><th>Reach</th><th>Clicks</th><th>Messages</th><th>Leads</th><th>Orders</th><th>Revenue</th><th>ROAS</th><th>CPA</th><th>Net Profit</th><th>ROI</th><th>Actions</th></tr></thead><tbody>{metrics.map((metric) => <tr key={metric.id} className="border-b last:border-0"><td className="py-2">{metric.metric_date}</td><td>{metric.spend}</td><td>{metric.impressions}</td><td>{metric.reach}</td><td>{metric.clicks}</td><td>{metric.messages}</td><td>{metric.leads}</td><td>{metric.orders}</td><td>{metric.revenue}</td><td><MetricValue value={metric.roas} /></td><td><MetricValue value={metric.cpa} /></td><td><MetricValue value={metric.net_profit} /></td><td><MetricValue value={metric.roi} suffix="%" /></td><td><div className="flex flex-wrap gap-2">{onEdit ? <button aria-label={`Edit metric ${metric.metric_date}`} className="rounded-lg border border-slate-300 px-3 py-2 font-semibold" onClick={() => onEdit(metric)}>Edit metric</button> : <span className="text-slate-400">Read-only</span>}{onDelete ? <button aria-label={`Delete metric ${metric.metric_date}`} className="rounded-lg border border-rose-200 px-3 py-2 font-semibold text-rose-700" onClick={() => onDelete(metric)}>Delete metric</button> : null}</div></td></tr>)}</tbody></table></div></section>; }
