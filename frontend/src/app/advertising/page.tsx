"use client";
import { useQuery } from "@tanstack/react-query";
import { useState } from "react";
import { AdMetricForm } from "@/features/advertising/components/ad-metric-form";
import { AdMetricTable } from "@/features/advertising/components/ad-metric-table";
import { AdvertisingDateRangeFilter } from "@/features/advertising/components/advertising-date-range-filter";
import { AdvertisingKpiCard } from "@/features/advertising/components/advertising-kpi-card";
import { AdvertisingTrendChart } from "@/features/advertising/components/advertising-trend-chart";
import { CampaignForm } from "@/features/advertising/components/campaign-form";
import { CampaignPerformanceTable } from "@/features/advertising/components/campaign-performance-table";
import { CampaignTable } from "@/features/advertising/components/campaign-table";
import { fetchAdCampaigns, fetchAdMetrics, fetchAdvertisingSummary, fetchAdvertisingTrend, fetchCampaignPerformance } from "@/services/advertising";
export default function AdvertisingPage() { const [workspaceId, setWorkspaceId] = useState(""); const [token, setToken] = useState(""); const [startDate, setStartDate] = useState(""); const [endDate, setEndDate] = useState(""); const enabled = Boolean(workspaceId); const campaigns = useQuery({ queryKey: ["ad-campaigns", workspaceId], queryFn: () => fetchAdCampaigns(workspaceId, token), enabled }); const metrics = useQuery({ queryKey: ["ad-metrics", workspaceId], queryFn: () => fetchAdMetrics(workspaceId, token), enabled }); const summary = useQuery({ queryKey: ["ad-summary", workspaceId, startDate, endDate], queryFn: () => fetchAdvertisingSummary(workspaceId, token, startDate, endDate), enabled }); const performance = useQuery({ queryKey: ["ad-performance", workspaceId, startDate, endDate], queryFn: () => fetchCampaignPerformance(workspaceId, token, startDate, endDate), enabled }); const trend = useQuery({ queryKey: ["ad-trend", workspaceId, startDate, endDate], queryFn: () => fetchAdvertisingTrend(workspaceId, token, startDate, endDate), enabled }); return <main className="min-h-screen bg-slate-100 p-6 text-slate-950"><div className="mx-auto grid max-w-7xl gap-6"><header className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-orange-600">Advertising & ROAS</p><h1 className="mt-2 text-3xl font-bold">Manual Advertising Dashboard</h1><p className="mt-1 text-slate-600">Track ad spend, leads, orders, revenue, CPA, CPL, ROAS, and owner-only profit metrics without Meta Ads API integration.</p></header><section className="grid gap-3 rounded-2xl bg-white p-4 shadow-sm md:grid-cols-2"><input className="rounded-md border border-slate-300 px-3 py-2" placeholder="Workspace ID" value={workspaceId} onChange={(event) => setWorkspaceId(event.target.value)} /><input className="rounded-md border border-slate-300 px-3 py-2" placeholder="Access token" value={token} onChange={(event) => setToken(event.target.value)} /></section><AdvertisingDateRangeFilter startDate={startDate} endDate={endDate} onStartDate={setStartDate} onEndDate={setEndDate} /><section className="grid gap-4 md:grid-cols-4"><AdvertisingKpiCard label="Spend" value={summary.data?.total_spend} /><AdvertisingKpiCard label="Revenue" value={summary.data?.total_revenue} /><AdvertisingKpiCard label="ROAS" value={summary.data?.roas} /><AdvertisingKpiCard label="CPA" value={summary.data?.average_cpa} /><AdvertisingKpiCard label="CPL" value={summary.data?.average_cpl} /><AdvertisingKpiCard label="Orders" value={summary.data?.total_orders} /><AdvertisingKpiCard label="Leads" value={summary.data?.total_leads} /><AdvertisingKpiCard label="Messages" value={summary.data?.total_messages} /><AdvertisingKpiCard label="Net Profit" value={summary.data?.total_net_profit} /><AdvertisingKpiCard label="ROI" value={summary.data?.roi ? `${summary.data.roi}%` : null} /></section><div className="grid gap-4 lg:grid-cols-2"><CampaignForm /><AdMetricForm /></div><CampaignTable campaigns={campaigns.data ?? []} /><AdMetricTable metrics={metrics.data ?? []} /><CampaignPerformanceTable rows={performance.data ?? []} /><AdvertisingTrendChart data={trend.data ?? []} /></div></main>; }
