# AI usage and cost controls

Before any provider call Sellora checks the platform feature flag, workspace AI enablement, role permissions, input size, workspace limits, and provider configuration. Usage events store request type, status, tokens, latency, and estimated cost.

Costs shown in UI/API are estimates unless provider billing is synchronized directly.
