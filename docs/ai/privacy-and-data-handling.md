# AI privacy and data handling

Direct messages may include personal data. Application logs must not include full raw conversation text. Safe logging is limited to identifiers, message length, safe hashes, provider/model, latency, token counts, and safe error codes.

Raw provider requests, headers, and responses are not persisted. Structured analysis, suggestions, rejected suggestions, usage events, and audit history are retained for workspace auditability.
