# Prompt versioning

Every prompt has a key, version, purpose, input schema, output schema, maximum context, model class, and safety instructions. Prompt versions are persisted on `ai_analyses`.

Changing prompt behavior requires a new version, for example `direct-message-analysis:v2`.
