# Team management

OWNER can manually add OWNER, MANAGER, or ANALYST users to the current workspace using email, full name, role, and temporary password. Existing users can be attached to another workspace without password overwrite. Duplicate membership in the same workspace is blocked.

Deactivation is workspace-level through workspace_user.is_active=false.
