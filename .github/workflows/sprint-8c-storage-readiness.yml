name: Sprint 8C Storage Readiness

on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/staging_import_storage_probe.py"
      - ".github/workflows/sprint-8c-storage-readiness.yml"

permissions:
  contents: read

jobs:
  storage-readiness:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      STAGING_API_URL: https://sellora-api-staging.onrender.com
      STAGING_OWNER_EMAIL: ${{ secrets.STAGING_OWNER_EMAIL }}
      STAGING_OWNER_PASSWORD: ${{ secrets.STAGING_OWNER_PASSWORD }}
      STAGING_TEST_WORKSPACE_ID: ${{ secrets.STAGING_TEST_WORKSPACE_ID }}

    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install probe dependency
        run: python -m pip install httpx
      - name: Compile probe
        run: python -m py_compile scripts/staging_import_storage_probe.py
      - name: Execute staging storage probe
        shell: bash
        run: |
          set +e
          python scripts/staging_import_storage_probe.py > /tmp/sprint-8c-storage-probe.log 2>&1
          code=$?
          cat /tmp/sprint-8c-storage-probe.log
          echo "$code" > artifacts/sprint-8c-storage-readiness-exit-code.txt
          exit 0
      - name: Validate sanitized evidence
        if: always()
        shell: bash
        run: |
          python - <<'PY'
          import json, os, pathlib
          report_path = pathlib.Path("artifacts/sprint-8c-storage-readiness.json")
          if not report_path.exists():
              raise SystemExit("Storage readiness report missing")
          text = report_path.read_text(encoding="utf-8")
          for value in (os.getenv("STAGING_OWNER_EMAIL", ""), os.getenv("STAGING_OWNER_PASSWORD", "")):
              if value and value in text:
                  raise SystemExit("Credential leaked into storage readiness artifact")
          report = json.loads(text)
          print(json.dumps(report, indent=2, ensure_ascii=False))
          PY
      - name: Upload sanitized evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sellora-sprint-8c-storage-readiness-${{ github.run_id }}
          path: artifacts
          if-no-files-found: error
          retention-days: 7
      - name: Enforce readiness decision
        if: always()
        shell: bash
        run: |
          code="$(cat artifacts/sprint-8c-storage-readiness-exit-code.txt 2>/dev/null || echo 99)"
          if [ "$code" != "0" ]; then
            exit 1
          fi
          python - <<'PY'
          import json
          report = json.load(open("artifacts/sprint-8c-storage-readiness.json", encoding="utf-8"))
          required = ("health", "login", "upload", "sheets")
          if any(report.get(key) != "PASS" for key in required):
              raise SystemExit(1)
          print("SPRINT 8C STORAGE READINESS: PASS")
          PY
