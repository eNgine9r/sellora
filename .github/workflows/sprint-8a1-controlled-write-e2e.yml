name: Sprint 8A.1 Controlled-write E2E (legacy disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "Legacy controlled-write workflow is disabled."
          echo "Use sprint-8a1-controlled-write-rerun.yml with verified runtime revision."
